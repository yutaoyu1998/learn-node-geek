{
  "name": "koa-project",
  "version": "1.0.0",
  "description": "app.use((ctx, next) => {\r     await next();\r })",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.4",
    "koa-mount": "^4.0.0"
  }
}
